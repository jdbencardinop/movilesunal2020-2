<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unal.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Multiplayer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameModeActivity"
            android:label="GameMode" />
        <activity android:name=".AndroidTicTacToeActivity"/>
<!--        <activity android:name=".Multiplayer" android:label="Multiplayer"/>&lt;!&ndash; Allow the Settings activity to be launched &ndash;&gt;-->
        <activity
            android:name=".Settings"
            android:label="Settings" />
    </application>

</manifest>